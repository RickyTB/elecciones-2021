import {StatsSidebar} from '../../components';

export interface DiagramPageProps {}

const DiagramPage: React.FC<DiagramPageProps> = () => {
  return (
    <div>
      <StatsSidebar />
    </div>
  );
};

export default DiagramPage;
